# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Auth -- JWT tokens + password hashing
PyJWT[crypto]>=2.9.0
bcrypt>=4.0.0

# Config & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0   # reads .env into typed Settings object
email-validator>=2.0.0     # required for Pydantic's EmailStr type

# Required by FastAPI to parse JSON request bodies
python-multipart>=0.0.9

# Rate limiting
slowapi>=0.1.9

# OIDC / SSO (optional -- enabled via OIDC_ENABLED in .env)
joserfc>=1.0.0
httpx>=0.27.0
itsdangerous>=2.2.0
