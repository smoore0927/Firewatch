# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.0

# Auth -- JWT tokens + password hashing
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Config & validation
pydantic>=2.0.0
pydantic-settings>=2.0.0   # reads .env into typed Settings object
email-validator>=2.0.0     # required for Pydantic's EmailStr type

# Required by FastAPI to parse JSON request bodies
python-multipart>=0.0.9
